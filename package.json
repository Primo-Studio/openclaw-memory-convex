{
  "name": "@primo/memory-convex",
  "version": "0.3.0",
  "private": true,
  "description": "Auto-recall and auto-capture agent memory via Convex (agentMemory table). Zero dependencies, zero cost.",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}